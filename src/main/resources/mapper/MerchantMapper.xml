<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sdust.shopping.mapper.MerchantMapper">
    
    <resultMap id="BaseResultMap" type="cn.edu.sdust.shopping.entity.Merchant">
        <id column="merchant_id" property="merchantId"/>
        <result column="merchant_name" property="merchantName"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        merchant_id, merchant_name, contact_person, contact_phone, address, status, create_time, update_time
    </sql>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
        WHERE merchant_id = #{merchantId}
    </select>
    
    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
    </select>
    
    <select id="findActiveMerchants" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM merchant
        WHERE status = 1
    </select>
    
    <insert id="insert" parameterType="cn.edu.sdust.shopping.entity.Merchant" useGeneratedKeys="true" keyProperty="merchantId">
        INSERT INTO merchant (merchant_name, contact_person, contact_phone, address, status)
        VALUES (#{merchantName}, #{contactPerson}, #{contactPhone}, #{address}, #{status})
    </insert>
    
    <update id="update" parameterType="cn.edu.sdust.shopping.entity.Merchant">
        UPDATE merchant
        <set>
            <if test="merchantName != null">merchant_name = #{merchantName},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE merchant_id = #{merchantId}
    </update>
    
    <delete id="deleteById">
        DELETE FROM merchant WHERE merchant_id = #{merchantId}
    </delete>
    
</mapper>
