<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sdust.shopping.mapper.CartMapper">
    
    <resultMap id="BaseResultMap" type="cn.edu.sdust.shopping.entity.Cart">
        <id column="cart_id" property="cartId"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <!-- 根据用户ID查询购物车 -->
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId}
    </select>
    
    <!-- 创建购物车 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="cartId">
        INSERT INTO cart (user_id) VALUES (#{userId})
    </insert>
    
    <!-- 删除购物车 -->
    <delete id="deleteById">
        DELETE FROM cart WHERE cart_id = #{cartId}
    </delete>
    
</mapper>
