<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sdust.shopping.mapper.OrderItemMapper">
    
    <resultMap id="BaseResultMap" type="cn.edu.sdust.shopping.entity.OrderItem">
        <id column="item_id" property="itemId"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        item_id, order_id, product_id, product_name, product_image, price, quantity, total_price
    </sql>
    
    <select id="findByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM order_item
        WHERE order_id = #{orderId}
    </select>
    
    <insert id="insert" parameterType="cn.edu.sdust.shopping.entity.OrderItem" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO order_item (order_id, product_id, product_name, product_image, price, quantity, total_price)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productImage}, #{price}, #{quantity}, #{totalPrice})
    </insert>
    
    <insert id="batchInsert">
        INSERT INTO order_item (order_id, product_id, product_name, product_image, price, quantity, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.price}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>
    
</mapper>
