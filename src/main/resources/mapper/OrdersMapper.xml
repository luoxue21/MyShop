<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sdust.shopping.mapper.OrdersMapper">
    
    <resultMap id="BaseResultMap" type="cn.edu.sdust.shopping.entity.Orders">
        <id column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="order_status" property="orderStatus"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="username" property="username"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        o.order_id, o.order_no, o.user_id, o.total_amount, o.receiver_name, o.receiver_phone,
        o.receiver_address, o.order_status, o.remark, o.create_time, o.update_time, u.username
    </sql>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        WHERE o.order_id = #{orderId}
    </select>
    
    <select id="findByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        WHERE o.order_no = #{orderNo}
    </select>
    
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        WHERE o.user_id = #{userId}
        ORDER BY o.create_time DESC
    </select>
    
    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        ORDER BY o.create_time DESC
    </select>
    
    <insert id="insert" parameterType="cn.edu.sdust.shopping.entity.Orders" useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO orders (order_no, user_id, total_amount, receiver_name, receiver_phone, receiver_address, order_status, remark)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{receiverName}, #{receiverPhone}, #{receiverAddress}, #{orderStatus}, #{remark})
    </insert>
    
    <update id="update" parameterType="cn.edu.sdust.shopping.entity.Orders">
        UPDATE orders
        <set>
            <if test="orderStatus != null">order_status = #{orderStatus},</if>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="receiverAddress != null">receiver_address = #{receiverAddress},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE order_id = #{orderId}
    </update>
    
    <delete id="deleteById">
        DELETE FROM orders WHERE order_id = #{orderId}
    </delete>
    
</mapper>
